import 'package:flame_forge2d/flame_forge2d.dart';

class PaddleControlPoint extends BodyComponent {
  // class for joining onto either side of the paddle
  // thiss allows raising the paddle from a single side, rather than the
  // paddle's origin (e.g. pulling one end up with a chain)
}
